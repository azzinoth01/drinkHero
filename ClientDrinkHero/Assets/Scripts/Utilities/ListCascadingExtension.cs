public static class ListCascadingExtension {


    //public static void AddWithCascading<T, S>(this IList<T> list, T add, S source) where T : ICascadable where S : ICascadable {
    //    list.Add(add);
    //    add.Cascadables.Add(source);
    //}
    //public static void RemoveWithCascading<T, S>(this IList<T> list, T remove, S source) where T : ICascadable where S : ICascadable {
    //    list.Remove(remove);
    //    remove.Cascadables.Remove(source);
    //}

    //public static void AddWithCascading<T, I, S>(this IDictionary<I, T> dictionary, I key, T add, S source) where T : ICascadable where S : ICascadable {

    //    dictionary.TryAdd(key, add);
    //    add.Cascadables.Add(source);
    //}
    //public static void RemoveWithCascading<T, I, S>(this IDictionary<I, T> dictionary, I key, S source) where T : ICascadable where S : ICascadable {

    //    dictionary.Remove(key, out T remove);
    //    remove.Cascadables.Remove(source);

    //}

}
